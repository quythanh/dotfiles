# QuyThanh's dotfile

### Installation

Requirements: `git`, `bash` or `zsh`.

Clone dotfile:

```bash
git clone https://github.com/quythanh/dotfiles ~/.dotfile
```

Change directory:

```bash
cd ~/.dotfiles
```

Run script:

```bash
sudo ./setup.sh
```
